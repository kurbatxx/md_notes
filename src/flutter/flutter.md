# Flutter

build runner
```
flutter pub run build_runner build --delete-conflicting-outputs
```

dep for freezed
```
dependencies:
    freezed_anotation:

dev_dependencies:
    freezed:
    build_runner:
    json_serializable:
```

